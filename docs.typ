#import "@preview/cetz:0.3.4": canvas, draw

#set par(justify: true)
#set text(10pt)
#set page(numbering: "1", columns:2, margin:1cm)

#{
  set text(35pt)
  place(
    top + center,
    scope: "parent",
    float: true,
    [*Jaris*]
  )
}
#outline()

= Introduction
Jaris is a 16 bit computer with TTL

#canvas({
  import draw: *
  
})
